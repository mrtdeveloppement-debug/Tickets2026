# 🔙 العودة لتسجيل الدخول بالبريد الإلكتروني

## ✅ تم إعادة كل شيء للحالة الأصلية

### التغييرات:
- ✅ تسجيل الدخول الآن بالبريد الإلكتروني (كما كان سابقاً)
- ✅ رقم الهاتف لا يزال المعرف الرئيسي للتذاكر
- ✅ اسم العميل لا يزال اختيارياً

---

## 🔧 خطوات الإصلاح

### الخطوة 1: تصحيح البريد الإلكتروني في قاعدة البيانات

**نفذ هذا في Supabase SQL Editor:**

```sql
-- تصحيح البريد الإلكتروني
UPDATE users 
SET email = 'admin@rimatel.mr'
WHERE email = 'admi@rimatel.mr';

-- التحقق
SELECT email, role FROM users WHERE role = 'admin';
```

### الخطوة 2: تأكد من وجود المستخدم في Authentication

1. اذهب إلى **Authentication** → **Users** في Supabase
2. ابحث عن المستخدم
3. تأكد أن البريد هو: `admin@rimatel.mr`

**إذا لم يكن موجوداً:**
- اضغط **Add user** → **Create new user**
- Email: `admin@rimatel.mr`
- Password: `admin123`
- ✅ Auto Confirm User
- اضغط **Create user**

### الخطوة 3: ربط المستخدم في جدول users

```sql
-- احصل على ID من Authentication
SELECT id, email FROM auth.users WHERE email = 'admin@rimatel.mr';

-- ثم نفذ (ضع الـ ID الذي حصلت عليه)
INSERT INTO users (id, email, full_name, role, is_active)
VALUES (
  'ضع-الـ-ID-هنا',
  'admin@rimatel.mr',
  'Administrateur RIMATEL',
  'admin',
  true
)
ON CONFLICT (id) DO UPDATE 
SET email = 'admin@rimatel.mr', role = 'admin';
```

---

## 🎯 تسجيل الدخول الآن

```
البريد الإلكتروني: admin@rimatel.mr
كلمة المرور: admin123
```

---

## 📋 ما تم الاحتفاظ به من التحديثات السابقة

### ✅ لا يزال يعمل:
1. **رقم الهاتف كمعرف رئيسي** - رقم الهاتف هو الحقل الأول في إنشاء التذكرة
2. **اسم العميل اختياري** - يمكن ترك حقل اسم العميل فارغاً
3. **أنواع الشكاوى** - جميع أنواع الشكاوى الـ 6 موجودة
4. **لوحة الإدارة** - إدارة المستخدمين والأدوار
5. **نوع الاشتراك LTE** - تم إضافته

### ❌ تم إلغاؤه:
1. **تسجيل الدخول باسم المستخدم** - عدنا للبريد الإلكتروني

---

## 🧪 اختبار

1. **أعد تحميل الصفحة** (F5)
2. **جرب تسجيل الدخول**:
   - البريد: `admin@rimatel.mr`
   - كلمة المرور: `admin123`
3. **يجب أن ينجح!**

---

## 🐛 إذا استمرت المشكلة

### المشكلة: "Invalid login credentials"

**الحل:**

```sql
-- 1. تحقق من المستخدمين في Auth
SELECT id, email FROM auth.users;

-- 2. تحقق من المستخدمين في جدول users
SELECT id, email, role FROM users;

-- 3. تأكد أن الـ ID متطابق في الجدولين
```

**إذا كان الـ ID مختلفاً:**

```sql
-- احذف من جدول users
DELETE FROM users WHERE email LIKE '%admin%';

-- أعد الإدراج بالـ ID الصحيح من auth.users
INSERT INTO users (id, email, full_name, role, is_active)
VALUES (
  'الـ-ID-من-auth.users',
  'admin@rimatel.mr',
  'Administrateur RIMATEL',
  'admin',
  true
);
```

---

## 📞 المساعدة

إذا واجهت أي مشكلة:
1. أخبرني بنتيجة: `SELECT id, email FROM auth.users;`
2. أخبرني بنتيجة: `SELECT id, email FROM users;`
3. سأعطيك الحل الدقيق!

---

**الحالة**: ✅ تم الرجوع لتسجيل الدخول بالبريد الإلكتروني

